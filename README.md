# My_Vector
A custom C++ student project that implements a template-based Vector container along with a user-defined numeric type (My_Float) using rational number representation, and a derived class (MySatur) that introduces saturation arithmetic.
